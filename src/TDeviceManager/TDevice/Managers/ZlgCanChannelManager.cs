using TDevice.Connection;
using TDevice.Connection.Zlg;

namespace TDevice.Managers;

public class ZlgCanChannelManager : ManagerBase<ZlgCanChannelManager, ZlgCanChannelBase>
{
    //#region 单例实现

    //private static Lazy<ZlgCanChannelManager> _instanceLazy =
    //    new Lazy<ZlgCanChannelManager>(() => new ZlgCanChannelManager());

    //public static ZlgCanChannelManager Instance => _instanceLazy.Value;

    //#endregion

    //#region 私有构造

    //private ZlgCanChannelManager()
    //{
        
    //}

    //#endregion

    //public Dictionary<string, ZlgCanChannelBase> ZlgCanChannels { get; set; } = new Dictionary<string, ZlgCanChannelBase>();
}